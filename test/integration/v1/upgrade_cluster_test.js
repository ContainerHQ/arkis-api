'use strict';

describe.skip('PATCH /clusters/:cluster_id', () => {

  // verify that all nodes are in upgrading state

  context('when cluster has no reachable node', () => {
    // it should have the same state
  });

  context('when cluster already has the lastest versions', () => {

  });

  context('when cluster id is invalid', () => {

  });

  // 401 unauthorized
});
